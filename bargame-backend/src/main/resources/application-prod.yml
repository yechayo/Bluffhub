spring:
  datasource:
    url: jdbc:mysql://115.159.127.205:3306/liar_bar?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 4thf*-hygu-J
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080
  ssl:
    enabled: false
# Netty WebSocket生产环境配置
netty:
  websocket:
    # WebSocket服务器端口
    port: 8091
    # WebSocket路径
    path: /ws
    # Boss线程数（生产环境可以适当增加）
    boss-threads: 2
    # Worker线程数（设置为CPU核心数）
    worker-threads: 4



logging:
  level:
    root: warn
    com.lb.net.netty: info  # Netty相关日志